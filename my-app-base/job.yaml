apiVersion: batch/v1
kind: Job
metadata:
  generateName: my-test-
  annotations:
    argocd.argoproj.io/hook: PreSync
spec:
  template:
    spec:
      containers:
      - name: echo
        image: alpine
        command: ["echo",  "This is a start!"]
      restartPolicy: Never
  backoffLimit: 4

